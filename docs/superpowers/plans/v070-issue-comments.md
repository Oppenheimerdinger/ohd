# v0.7.0 issue comments — DRAFTS for the maintainer to post

These are texts, not actions. Nothing here has been posted; the implementation
deliberately does not comment on or close public issues. Post (or edit) them by
hand at release time.

Issues #21–#29 are answered by the release itself and need no comment beyond
whatever the maintainer wants to say when closing them. The three below need a
comment because their disposition is NOT "shipped in v0.7.0".

---

## #15 — close

> Shipped in v0.5.23: the description was re-aimed at the act rather than the
> topic, which is what this issue asked for. Closing.
>
> Two residuals from the discussion are recorded as deliberately NOT shipped,
> so they are not lost:
>
> - the thin-wrapper framing, and
> - the table-row phrasing that still reads as if the instrument were the
>   deliverable.
>
> Both stay open as recorded non-ships rather than as work. Revisit trigger: a
> field session that reads the table row and still calls the instrument bare —
> that is the observation that would turn either of them into a change.

---

## #14 — correct the comment map, then close

The earlier comment on this issue mapped the three proposals wrongly. The
correction is the point of the comment.

> Correcting the disposition map on this issue, because the previous comment
> had it wrong:
>
> - **Proposal A (empty-plan commit gate) — REJECTED, with measurement.** Not
>   deferred and not overlooked. The rejection and the measurement behind it
>   are recorded in v0.5.23's NOT-shipped block.
> - **Proposal B (micro-edit work-size) — SHIPPED in v0.5.23.**
> - **Proposal C (autonomous-mandate lane rule) — SHIPPED in v0.5.23.**
>
> So two of three shipped nine months of releases ago and one was rejected on
> evidence; none is outstanding. Closing.

---

## #7 — keep OPEN, with the disposition stated

Deliberately not re-triaged silently. The comment exists so the issue stops
looking untouched.

> Keeping this open, with the disposition written down so it is not
> re-triaged from scratch every time someone reads it.
>
> The preferred future form is a ONE-TIME full-history sweep, not an ongoing
> gate — the failure this describes is a fossil problem, and a permanent check
> for it would cost more than the fossils do. That is now a backlog entry
> rather than a plan.
>
> Revisit trigger: the next fossil incident. If one lands, this issue is the
> place it gets recorded, and the sweep becomes worth scheduling.
